<h1>User pages </h1>
<% @users.each do |item| %>
   <%= link_to user_path(item) do%>
      <div>
      <div><img src=<%= item.photo %>></div>
      <div>
      <h3><%= item.name %></h3>
      <p>Number of post: <%= item.postsCounter %> </p>
      </div>
      </div>
   <% end %>
<% end %>